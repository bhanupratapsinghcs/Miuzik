import { createMuiTheme } from "@material-ui/core"

const theme = createMuiTheme({
    palette: {
        type: "dark",
        primary: {
            main: '#000'
        },
        secondary: {
            main: '#131313'
        }
    }

})
export default theme;